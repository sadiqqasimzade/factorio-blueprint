import React from 'react'
import styles from "./ImageOverView.module.scss";

type Props = {}

const ImageOverView = (props: Props) => {
  return (
    <div>
        IOV
    </div>
  )
}

export default ImageOverView